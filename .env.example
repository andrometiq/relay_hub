# Export these only when using an externally managed PostgreSQL 18 server.
# Local ./relay install uses its own private Unix-socket PostgreSQL cluster.
# DATABASE_URL=postgres://relay_user:CHANGE_ME@127.0.0.1:5432/relay_hub?sslmode=verify-full
RELAY_ADDR=127.0.0.1:18081
